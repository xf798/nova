# AI 技能比武大赛 · 参赛作品

## 作品名称

**Nova AI 工作台 — 桌面端 + 企微远程 + 自动化流水线三位一体的 AI Native 工作方式**

## 参赛赛道

产品创新

## 参赛人

王晓峰 · PBG 前端

---

## 一、项目概述

Nova 是一个 **AI Native 工作台**，将 AI 从"聊天工具"升级为"工作操作系统"。它不是单点的 AI 应用，而是一个完整的 AI 工作平台，覆盖三大场景：

| 场景 | 形态 | 核心价值 |
|------|------|----------|
| 桌面办公 | Nova 桌面应用（macOS） | AI 驱动的本地工作台，一句话操控全部工作流 |
| 移动/远程办公 | 企微机器人 | 手机上随时对话 AI，不在电脑前也能推进工作 |
| 自动化生产 | AutoProgram 流水线 | PRD → 设计 → 代码，端到端全自动 |

**一句话总结：让 AI 不只是回答问题，而是真正帮你做事、替你做事、自动做事。**

---

## 二、Nova 桌面应用 — AI 工作台

### 技术架构

- **前端**：React 18 + TypeScript + TailwindCSS
- **后端**：Rust + Tauri 2（高性能、低资源占用）
- **AI 引擎**：Kiro CLI（Amazon AI Agent）
- **形态**：macOS 桌面应用

### 核心能力

#### 1. Action 机制 — AI 不只聊天，还能操控

Nova 中的 AI 不是纯文本对话。通过 Action 机制，AI 可以直接操控应用：

- `nav.goto` — 跳转页面
- `ui.notify` — 发送通知
- `ui.screenshot` — 截取当前屏幕
- `autoprogram.start` — 启动自动化流水线
- `connector.switch` — 切换 AI 模型
- ...更多 actions 可由插件动态注册

**效果**：用户说"启动流水线"，AI 不是给一段说明文字，而是直接执行流水线。

#### 2. Skills 系统 — 垂直场景深度集成

已落地的 Skills：

| Skill | 能力 | 使用场景 |
|-------|------|----------|
| **Code-Deploy** | Gerrit Code-Review + Jenkins 部署 | "帮我 +2"、"发到 dev 环境" |
| **Nova-Tasks** | 任务管理（直接读写本地 JSON） | "帮我记个 TODO"、"看看待办" |
| **TCHub** | 项目文档/工作流管理 | "上传 PRD"、"查看 workstream 状态" |
| **System-Control** | macOS 系统控制（音量/亮度/锁屏/睡眠/WiFi/蓝牙/应用管理/勿扰） | "锁屏"、"音量调大"、"打开 Safari"、"关掉 WiFi" |

每个 Skill 包含完整的触发词、执行脚本和配置，AI 根据用户意图自动匹配。

**System-Control 亮点**：一句话控制 Mac，支持电源（锁屏/睡眠/关屏）、音量、亮度、勿扰模式、应用启动/退出、WiFi/蓝牙开关、系统信息查询。通过 osascript + pmset + networksetup 原生实现，高危操作（退出应用、关 WiFi）需用户确认，不支持关机/重启等不可逆操作。

#### 3. Connector 架构 — 多模型即插即用

支持切换不同 AI 后端（Kiro CLI、OpenAI、本地模型），每个会话独立 connector 实例，支持进程池管理、超时清理。

#### 4. Plugin 系统 — 可扩展的功能模块

插件通过 `manifest.json` 声明、动态加载，支持：
- 注册自定义 Actions
- 渲染侧边栏 UI
- 访问宿主存储和事件系统
- 创建独立 AI Connector（每个插件可驱动独立的 AI 会话）

#### 5. 企微 WebSocket 集成（Rust 原生）

Nova 桌面端内置 Rust 实现的企微 WebSocket 长连接：
- 直连 `wss://openws.work.weixin.qq.com`
- 指数退避自动重连
- 消息持久化（未回复消息重启后自动恢复）
- 前端实时状态展示

### 实际使用数据

从 7月15日 启动到现在（7月22日），通过 AI 驱动完成了 **10 个功能任务**：

| 完成日期 | 任务 |
|----------|------|
| 7/20 | Nova 关键链路添加日志 |
| 7/21 | Bug: 会话上下文断裂修复 |
| 7/21 | 重构 thinking + toolcall 展示为 ChatGPT 风格 |
| 7/21 | Toolcall 展示优化（最短显示时长、loading 连续性） |
| 7/21 | 侧边栏改造（预制选项固定展示） |
| 7/21 | 会话对应的 Kiro CLI 进程管理/超时清理 |
| 7/22 | 历史消息引用到对话内 |
| 7/22 | 截图图片直接回复到消息内 |
| 7/22 | 发送图片可再次引用到 input |
| 7/22 | ai-develop-team 根目录清理（20G → 5.5G） |

**7 天完成了传统方式可能需要 2-3 周的工作量**，效率提升约 3x。

---

## 三、企微机器人 — 移动端远程办公

### 核心价值

不在电脑前时，通过企微 @机器人 即可远程操控工作流。实现了"AI 跟着人走，不是人跟着电脑走"。

### 技术方案

```
企微消息 → WebSocket 长连接 → 消息解析 → 访问控制 → 会话管理 → AI 调用 → 回复
```

独立 Node.js 服务，部署在服务器，7×24 在线。

### 核心特性

| 特性 | 说明 |
|------|------|
| **会话记忆** | 三层上下文：长期记忆 + 会话摘要 + 工作记忆（最近 20 轮） |
| **自动摘要** | 对话过长时自动压缩历史为摘要，保持上下文连贯 |
| **长期记忆提取** | 自动从对话中提取用户偏好/决策，跨会话记忆 |
| **防并发** | 同一会话同时只处理一条消息，避免 race condition |
| **访问控制** | 用户白名单 + 群组白名单 + 管理员权限 |
| **错误友好** | 超时、重复发送、AI 不可用等场景都有用户友好提示 |
| **持久化** | 会话数据持久化到磁盘，服务重启不丢失上下文 |

### 使用场景示例

- 通勤路上 @机器人："帮我看看今天的待办"
- 会议中 @机器人："刚才讨论的方案帮我记一下：xxx"
- 晚上在家 @机器人："帮我 review 一下 outgoing 的代码"
- 周末想到一个点子 @机器人："记个 TODO：下周优先做 xxx"

---

## 四、AutoProgram — PRD 到代码的全自动流水线

### 核心理念

传统软件开发流程：PM 写 PRD → UX 做设计 → Dev 写代码，每个环节都需要人工交接。

AutoProgram 将这三个阶段完全自动化：

```
PRD（产品需求文档）
    ↓ AI PM Agent
设计方案（组件结构 + 交互规范 + 状态管理）
    ↓ AI UX Agent  
代码实现（完整可运行的前端代码）
    ↓ AI Dev Agent
交付产物
```

### 技术实现

- **三个独立 AI Agent**：PM/UX/Dev 各有独立工作目录和 AI 会话
- **状态机引擎**：流水线有完整的状态管理（idle → running → paused → completed/failed）
- **阶段间文件自动流转**：PM 产出自动复制到 UX 输入，UX 产出自动复制到 Dev 输入
- **TCHub 集成**：自动从 TCHub 拉取 PRD，执行结果同步回 TCHub
- **用户决断机制**：失败时暂停等待用户决策（重试/跳过），不是简单的失败退出
- **可视化面板**：Nova 侧边栏中直接查看流水线进度、日志、产物

### 已验证的 Workstream

从 TCHub 数据可见，已有多个真实项目通过 AutoProgram 流转：

- 「自动流测试 Demo」— 端到端自动化验证
- 「WS6 维度规则两步式弹窗」— 真实业务需求，已进入 implementation 阶段
- 「native-h5 参考 web 补齐功能」— 5 个 Phase 实现完成

### 潜力与展望

| 维度 | 当前 | 未来 |
|------|------|------|
| 输入 | 本地 PRD / TCHub 文档 | 语音描述 → PRD → 代码 |
| 流水线 | 顺序执行 | 并行 + 增量更新 |
| 验证 | 人工验收 | 自动化 E2E 测试 |
| 部署 | 手动 | 一键发布到 dev/test 环境 |

---

## 五、三位一体的协同价值

```
                    ┌─────────────────────────┐
                    │     TCHub (需求中心)      │
                    └──────────┬──────────────┘
                               │
           ┌───────────────────┼───────────────────┐
           │                   │                   │
    ┌──────▼──────┐    ┌──────▼──────┐    ┌──────▼──────┐
    │ Nova 桌面端  │    │  企微机器人  │    │ AutoProgram │
    │ (深度操控)   │    │ (随时可达)   │    │ (自动生产)   │
    └──────┬──────┘    └──────┬──────┘    └──────┬──────┘
           │                   │                   │
           └───────────────────┼───────────────────┘
                               │
                    ┌──────────▼──────────────┐
                    │   统一 AI Agent 能力层    │
                    │  (Skills / Actions /     │
                    │   Memory / Connectors)   │
                    └─────────────────────────┘
```

**协同场景举例**：

1. **在企微上** @机器人 说"帮我看看 TCHub 上的新需求" → 机器人反馈有新 PRD
2. **回到电脑前**，在 Nova 中说"启动 AutoProgram" → 流水线自动从 TCHub 拉取 PRD，依次生成设计和代码
3. 流水线完成后，在 Nova 中说"帮我 +2" → Gerrit Code-Review 批量通过
4. 最后说"发到 dev" → Jenkins 自动构建部署

**从需求发现 → 代码生成 → 代码审查 → 环境部署，全程 AI 驱动，人只做决策。**

---

## 六、技术亮点总结

| 亮点 | 说明 |
|------|------|
| 🏗️ 全栈自研 | Rust + React + Node.js，无低代码/无拖拽，纯 AI Agent 架构 |
| 🔌 可扩展 | Plugin + Skill + Action 三层扩展体系 |
| 🧠 有记忆 | 用户偏好长期记忆 + 会话摘要 + 跨设备同步 |
| 🔗 强集成 | Gerrit + Jenkins + TCHub + 企微，全链路打通 |
| 🤖 自动化 | 不只是对话辅助，是真正的端到端自动生产 |
| 📱 全场景 | 桌面 + 移动 + 后台，7×24 AI 可达 |

---

## 七、效率提升数据

| 指标 | 数值 |
|------|------|
| 项目启动 → 首版可用 | 7 天（7.15 - 7.22） |
| AI 驱动完成的功能数 | 10 个 |
| 代码仓库清理 | 20G → 5.5G（-75%） |
| 预估效率提升 | 3x（7 天完成传统 2-3 周工作量） |
| 企微机器人在线时长 | 7×24 |
| 流水线单次运行覆盖 | PRD + 设计 + 代码全链路 |

---

## 八、后续规划

1. **定时任务引擎** — 支持 cron 表达式调度自动化任务
2. **流水线并行优化** — 多 workstream 并行执行
3. **自动化测试集成** — 代码生成后自动跑测试验证
4. **团队协同** — 多人共享 Nova 实例，协同管理 workstream
5. **更多 Skill 扩展** — JIRA 同步、Confluence 文档、监控告警处理

---

*作品持续迭代中。以上内容基于截至 2026-07-22 的实际运行数据。*
